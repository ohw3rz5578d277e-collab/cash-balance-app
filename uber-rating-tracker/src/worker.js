const corsHeaders = {
  "Access-Control-Allow-Origin": "*",
  "Access-Control-Allow-Methods": "GET,POST,DELETE,OPTIONS",
  "Access-Control-Allow-Headers": "Content-Type"
};

const appIcon = `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512"><rect width="512" height="512" rx="108" fill="#080a0c"/><circle cx="256" cy="206" r="144" fill="none" stroke="#04d477" stroke-width="22" stroke-linecap="round"/><text x="256" y="238" text-anchor="middle" font-size="132" font-family="Arial" font-weight="900" fill="#fff">👍</text><text x="256" y="365" text-anchor="middle" font-size="96" font-family="Arial, sans-serif" font-weight="900" fill="#04d477">評価</text><text x="256" y="420" text-anchor="middle" font-size="34" font-family="Arial" font-weight="800" fill="#fff" letter-spacing="5">TRACKER</text></svg>`;

function json(data, status = 200) {
  return new Response(JSON.stringify(data), { status, headers: { "content-type": "application/json; charset=UTF-8", ...corsHeaders } });
}
function html(body) { return new Response(body, { headers: { "content-type": "text/html; charset=UTF-8" } }); }
function text(body, type = "text/plain; charset=UTF-8") { return new Response(body, { headers: { "content-type": type } }); }
function uid() { return crypto.randomUUID(); }

async function reverseGeocode(lat, lng) {
  try {
    const api = `https://nominatim.openstreetmap.org/reverse?format=jsonv2&lat=${encodeURIComponent(lat)}&lon=${encodeURIComponent(lng)}&zoom=18&addressdetails=1`;
    const r = await fetch(api, { headers: { "User-Agent": "uber-rating-tracker/1.0" } });
    if (!r.ok) return null;
    const d = await r.json();
    const a = d.address || {};
    const name = d.name || a.shop || a.amenity || a.restaurant || a.fast_food || a.convenience || a.building || a.road || d.display_name || "現在地";
    const area = a.suburb || a.city_district || a.quarter || a.neighbourhood || a.city || a.town || a.village || "";
    return { name, area, display_name: d.display_name || "", provider: "nominatim" };
  } catch (e) {
    return null;
  }
}

export default {
  async fetch(request, env) {
    const url = new URL(request.url);
    if (request.method === "OPTIONS") return new Response(null, { status: 204, headers: corsHeaders });
    if (request.method === "GET" && url.pathname === "/") return html(page());
    if (request.method === "GET" && url.pathname === "/icon.svg") return text(appIcon, "image/svg+xml; charset=UTF-8");
    if (request.method === "GET" && url.pathname === "/manifest.json") return json({ name: "Uber Rating Tracker", short_name: "評価アプリ", start_url: "/", display: "standalone", background_color: "#080a0c", theme_color: "#028760", icons: [{ src: "/icon.svg", sizes: "any", type: "image/svg+xml", purpose: "any maskable" }] });
    if (request.method === "GET" && url.pathname === "/sw.js") return text("self.addEventListener('install',e=>self.skipWaiting());self.addEventListener('activate',e=>self.clients.claim());", "application/javascript; charset=UTF-8");
    if (url.pathname === "/api/health") return json({ ok: true, service: "uber-rating-tracker", build: "strong-delivery-button-v10", hasDb: !!env.DB, time: new Date().toISOString() });
    if (url.pathname === "/api/reverse-geocode") {
      const lat = url.searchParams.get("lat");
      const lng = url.searchParams.get("lng");
      if (!lat || !lng) return json({ ok: false, error: "lat_lng_required" }, 400);
      const result = await reverseGeocode(lat, lng);
      return json({ ok: true, result });
    }
    if (!env.DB) return json({ ok: false, error: "DB_NOT_CONNECTED" }, 503);
    if (url.pathname === "/api/ratings" && request.method === "GET") {
      const rows = await env.DB.prepare("SELECT * FROM rating_snapshots ORDER BY recorded_at DESC LIMIT 100").all();
      return json({ ok: true, items: rows.results || [] });
    }
    if (url.pathname === "/api/ratings" && request.method === "POST") {
      const body = await request.json();
      const now = Date.now();
      const totalGood = Number(body.total_good || 0);
      const totalBad = Number(body.total_bad || 0);
      const satisfaction = totalGood + totalBad > 0 ? Math.round(totalGood / (totalGood + totalBad) * 100) : 0;
      const prev = await env.DB.prepare("SELECT total_good,total_bad FROM rating_snapshots ORDER BY recorded_at DESC LIMIT 1").first();
      const deltaGood = prev ? totalGood - Number(prev.total_good || 0) : 0;
      const deltaBad = prev ? totalBad - Number(prev.total_bad || 0) : 0;
      const id = uid();
      await env.DB.prepare(`INSERT INTO rating_snapshots (id,recorded_at,satisfaction,merchant_good,merchant_bad,customer_good,customer_bad,total_good,total_bad,delta_good,delta_bad,source_device,note,created_at,updated_at) VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)`).bind(id, body.recorded_at || now, satisfaction, 0, 0, 0, 0, totalGood, totalBad, deltaGood, deltaBad, String(body.source_device || ""), String(body.note || ""), now, now).run();
      return json({ ok: true, id, satisfaction, delta_good: deltaGood, delta_bad: deltaBad });
    }
    if (url.pathname.startsWith("/api/ratings/") && request.method === "DELETE") {
      const id = decodeURIComponent(url.pathname.split("/").pop());
      await env.DB.prepare("DELETE FROM rating_snapshots WHERE id = ?").bind(id).run();
      return json({ ok: true });
    }
    if (url.pathname === "/api/ratings" && request.method === "DELETE") {
      await env.DB.prepare("DELETE FROM rating_snapshots").run();
      return json({ ok: true });
    }
    if (url.pathname === "/api/deliveries" && request.method === "GET") {
      const rows = await env.DB.prepare("SELECT * FROM deliveries ORDER BY completed_at DESC LIMIT 100").all();
      return json({ ok: true, items: rows.results || [] });
    }
    if (url.pathname === "/api/deliveries" && request.method === "POST") {
      const body = await request.json();
      const now = Date.now();
      const id = uid();
      await env.DB.prepare(`INSERT INTO deliveries (id,completed_at,store_name,area,memo,lat,lng,created_at,updated_at) VALUES (?,?,?,?,?,?,?,?,?)`).bind(id, body.completed_at || now, String(body.store_name || ""), String(body.area || ""), String(body.memo || "ワンタップ記録"), body.lat ?? null, body.lng ?? null, now, now).run();
      return json({ ok: true, id });
    }
    if (url.pathname.startsWith("/api/deliveries/") && request.method === "DELETE") {
      const id = decodeURIComponent(url.pathname.split("/").pop());
      await env.DB.prepare("DELETE FROM deliveries WHERE id = ?").bind(id).run();
      return json({ ok: true });
    }
    if (url.pathname === "/api/deliveries" && request.method === "DELETE") {
      await env.DB.prepare("DELETE FROM deliveries").run();
      return json({ ok: true });
    }
    return json({ ok: false, error: "NOT_FOUND" }, 404);
  }
};

function page() {
  return `<!doctype html><html lang="ja"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover"><title>評価アプリ</title><link rel="manifest" href="/manifest.json"><link rel="icon" href="/icon.svg"><link rel="apple-touch-icon" href="/icon.svg"><meta name="theme-color" content="#028760"><meta name="apple-mobile-web-app-capable" content="yes"><meta name="apple-mobile-web-app-title" content="評価アプリ"><script src="https://unpkg.com/tesseract.js@5/dist/tesseract.min.js"></script><style>
:root{--bg:#080a0c;--card:#141820;--card2:#1f2530;--line:#2d3440;--green:#04b873;--green2:#028760;--red:#ff5d5d;--blue:#2b64ff;--muted:#9aa3b2;--text:#fff}*{box-sizing:border-box;-webkit-tap-highlight-color:transparent}body{margin:0;background:radial-gradient(circle at top,#123326 0,#080a0c 38%);color:var(--text);font-family:-apple-system,BlinkMacSystemFont,'Noto Sans JP',sans-serif;padding:12px 12px 92px;user-select:none;-webkit-user-select:none}.top{display:flex;justify-content:space-between;align-items:center;margin-bottom:10px}.title{font-size:18px;font-weight:900}.sync{font-size:12px;border:1px solid #245c45;background:#10261d;color:#69efa9;border-radius:999px;padding:7px 10px}.hero{border:1px solid #245c45;background:linear-gradient(160deg,#06291e,#141820 65%);border-radius:26px;padding:18px;margin-bottom:12px;box-shadow:0 12px 28px rgba(0,0,0,.25)}.cap{color:var(--muted);font-size:12px;font-weight:800}.sat{font-size:68px;font-weight:950;letter-spacing:-4px;line-height:1}.counts{display:grid;grid-template-columns:1fr 1fr;gap:10px;margin-top:12px}.box{background:rgba(255,255,255,.06);border:1px solid rgba(255,255,255,.08);border-radius:18px;padding:13px}.num{font-size:32px;font-weight:950}.ok{color:#5be39a}.bad{color:var(--red)}.statusRow{display:grid;grid-template-columns:1.3fr .7fr;gap:10px;margin-bottom:12px}.miniCard{background:#111720;border:1px solid var(--line);border-radius:18px;padding:13px}.delta{font-size:21px;font-weight:950;margin-top:5px}.actions{display:grid;grid-template-columns:1fr 1fr;gap:10px;margin-bottom:12px}.big{border:0;border-radius:24px;min-height:108px;padding:18px;text-align:left;color:#fff;font-size:21px;font-weight:950;box-shadow:0 10px 24px rgba(0,0,0,.28);touch-action:manipulation;user-select:none;-webkit-user-select:none}.big small{display:block;font-size:12px;opacity:.82;margin-top:8px}.save{background:linear-gradient(135deg,var(--green),var(--green2));position:relative;overflow:hidden}.save.mainSave{grid-column:1/-1;min-height:138px;font-size:30px;padding:24px;box-shadow:0 0 0 1px rgba(4,212,119,.32),0 0 26px rgba(4,212,119,.28),0 18px 38px rgba(0,0,0,.35);animation:pulseSave 1.9s ease-in-out infinite}.save.mainSave small{font-size:14px}.save.mainSave:before{content:"";position:absolute;inset:-40%;background:linear-gradient(120deg,transparent 38%,rgba(255,255,255,.28) 48%,transparent 58%);transform:translateX(-65%) rotate(10deg);animation:shineSave 2.8s ease-in-out infinite}.save.mainSave.saving{opacity:.68;pointer-events:none;transform:scale(.985);animation:none}.save.mainSave.saving:after{content:"保存中...";position:absolute;right:18px;bottom:18px;font-size:14px;background:rgba(0,0,0,.28);padding:8px 12px;border-radius:999px}.ocr{background:linear-gradient(135deg,var(--blue),#182a70)}@keyframes pulseSave{0%,100%{filter:brightness(1);box-shadow:0 0 0 1px rgba(4,212,119,.32),0 0 22px rgba(4,212,119,.24),0 18px 38px rgba(0,0,0,.35)}50%{filter:brightness(1.1);box-shadow:0 0 0 1px rgba(4,212,119,.55),0 0 42px rgba(4,212,119,.42),0 18px 38px rgba(0,0,0,.35)}}@keyframes shineSave{0%{transform:translateX(-80%) rotate(10deg)}45%,100%{transform:translateX(80%) rotate(10deg)}}.alert{display:none;border-radius:22px;padding:16px;margin-bottom:12px;font-size:20px;font-weight:950}.alert.show{display:block}.alert.good{background:#102d21;border:1px solid #245c45;color:#5be39a}.alert.badalert{background:#35171b;border:1px solid #6b2a33;color:#ff8a8a}.tabs{position:fixed;left:10px;right:10px;bottom:8px;background:rgba(8,10,12,.95);border:1px solid var(--line);border-radius:20px;padding:8px;display:grid;grid-template-columns:repeat(4,1fr);gap:7px;backdrop-filter:blur(12px)}.tab{border:0;border-radius:14px;background:#202631;color:#fff;padding:12px 4px;font-size:12px;font-weight:900}.screen{display:none}.screen.active{display:block}.card{background:rgba(20,24,32,.96);border:1px solid var(--line);border-radius:20px;padding:15px;margin-bottom:12px}button{width:100%;border:0;border-radius:16px;background:var(--green2);color:#fff;padding:15px;font-size:16px;font-weight:900;margin-top:10px}button.secondary{background:#252c38}button.danger{background:#633039}input,textarea{width:100%;background:#10151d;border:1px solid var(--line);border-radius:14px;color:#fff;font-size:17px;padding:14px;margin-top:10px;user-select:text;-webkit-user-select:text}textarea{min-height:74px}.two{display:grid;grid-template-columns:1fr 1fr;gap:10px}.hint{font-size:13px;color:var(--muted);line-height:1.7}.row{border-bottom:1px solid var(--line);padding:11px 0}.rowTop{display:flex;justify-content:space-between;gap:10px;align-items:flex-start}.smallDel{width:auto;margin:0;background:#633039;padding:8px 10px;font-size:12px;border-radius:10px}.file{display:none}.toast{display:none;position:fixed;top:10px;left:12px;right:12px;background:#202631;border:1px solid var(--line);border-radius:16px;padding:13px;z-index:9;font-size:14px}.toast.show{display:block}.empty{color:var(--muted);text-align:center;padding:16px}.details{display:none}.details.open{display:block}
</style></head><body><div id="toast" class="toast"></div><div class="top"><div class="title">評価アプリ</div><div id="sync" class="sync">確認中</div></div>
<section id="home" class="screen active"><div id="changeAlert" class="alert"></div><div class="hero"><div class="cap">今の満足度</div><div id="sat" class="sat">--%</div><div class="counts"><div class="box"><div class="cap">GOOD</div><div id="good" class="num ok">--</div></div><div class="box"><div class="cap">BAD</div><div id="bad" class="num bad">--</div></div></div></div><div class="statusRow"><div class="miniCard"><div class="cap">前回との差分</div><div id="delta" class="delta">--</div></div><div class="miniCard"><div class="cap">状態</div><div id="mood" class="delta">--</div></div></div><div class="actions"><button id="quickSave" class="big save mainSave">配達完了<small>強タップ保存 / GPSで店舗候補取得</small></button><button id="pickFromHome" class="big ocr">評価を読む<small>自動OCR保存</small></button><input id="homeFile" class="file" type="file" accept="image/*"></div><div class="card"><div class="cap">直近配達候補</div><div id="candidate" class="hint">BAD増加時に直近の配達候補を表示します。</div></div><div class="card"><div class="cap">今日の記録</div><div class="two"><div><div class="hint">配達</div><div id="todayD" class="num">0</div></div><div><div class="hint">評価</div><div id="todayR" class="num">0</div></div></div></div></section>
<section id="ocr" class="screen"><div class="card"><div class="cap">評価スクショOCR</div><div class="hint">スクショ選択後、数字が読めたら自動保存します。違う場合だけ修正してください。</div><button id="pickImage" class="ocr">スクショを選ぶ</button><input id="imageFile" class="file" type="file" accept="image/*"><div id="ocrBox"></div></div><div class="card"><div class="cap">手動入力</div><div class="two"><input id="manualGood" type="number" placeholder="GOOD"><input id="manualBad" type="number" placeholder="BAD"></div><button id="manualSave">保存</button></div></section>
<section id="delivery" class="screen"><div class="card"><div class="cap">配達記録</div><button id="quickSave2" class="big save mainSave">今すぐ記録<small>強タップ保存 / GPSで店舗候補取得</small></button><button class="secondary" id="toggleDetail">詳細入力</button><div id="detail" class="details"><button class="secondary" id="gpsFill">GPSで店舗候補を取得</button><input id="store" placeholder="店舗名"><input id="area" placeholder="エリア"><textarea id="memo" placeholder="メモ"></textarea><button id="detailSave">詳細つきで保存</button></div></div></section>
<section id="history" class="screen"><div class="card"><div class="cap">履歴管理</div><div class="two"><button class="danger" id="clearRatings">評価履歴を削除</button><button class="danger" id="clearDeliveries">配達履歴を削除</button></div><button class="danger" id="clearAll">全履歴を削除</button></div><div class="card"><div class="cap">評価履歴</div><div id="ratingList"></div></div><div class="card"><div class="cap">配達履歴</div><div id="deliveryList"></div></div></section>
<div class="tabs"><button class="tab" data-tab="home">ホーム</button><button class="tab" data-tab="ocr">OCR</button><button class="tab" data-tab="delivery">配達</button><button class="tab" data-tab="history">履歴</button></div>
<script>
if('serviceWorker'in navigator)navigator.serviceWorker.register('/sw.js').catch(()=>{});const $=id=>document.getElementById(id);let ratings=[],deliveries=[],dbMode=false,deliveryLock=false;const fmt=t=>new Date(Number(t)).toLocaleString('ja-JP',{month:'2-digit',day:'2-digit',hour:'2-digit',minute:'2-digit'});function buzz(p=[40]){try{navigator.vibrate&&navigator.vibrate(p)}catch(e){}}function toast(m){$('toast').textContent=m;$('toast').classList.add('show');setTimeout(()=>$('toast').classList.remove('show'),2300)}function sign(v){v=Number(v||0);return v>0?'+'+v:String(v)}function day0(){const d=new Date();d.setHours(0,0,0,0);return d.getTime()}async function api(path,opt){const r=await fetch(path,opt);if(!r.ok)throw new Error(String(r.status));return r.json()}function localLoad(){ratings=JSON.parse(localStorage.getItem('ratings')||'[]');deliveries=JSON.parse(localStorage.getItem('deliveries')||'[]')}function localSave(){localStorage.setItem('ratings',JSON.stringify(ratings));localStorage.setItem('deliveries',JSON.stringify(deliveries))}async function load(){try{const h=await api('/api/health');dbMode=!!h.hasDb;$('sync').textContent=dbMode?'同期OK':'端末保存';if(dbMode){ratings=(await api('/api/ratings')).items||[];deliveries=(await api('/api/deliveries')).items||[]}else localLoad()}catch(e){dbMode=false;$('sync').textContent='端末保存';localLoad()}render()}function showAlert(item){const a=$('changeAlert');if(!item){a.className='alert';a.textContent='';return}if(Number(item.delta_bad)>0){a.className='alert show badalert';a.textContent='👎 BAD '+sign(item.delta_bad)+' 増加';buzz([80,40,80])}else if(Number(item.delta_good)>0){a.className='alert show good';a.textContent='👍 GOOD '+sign(item.delta_good)+' 増加';buzz([50])}else{a.className='alert';a.textContent=''}}function esc(s){return String(s||'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}function render(){const latest=ratings[0];if(latest){$('sat').textContent=(latest.satisfaction||Math.round(latest.total_good/(latest.total_good+latest.total_bad)*100)||0)+'%';$('good').textContent=latest.total_good;$('bad').textContent=latest.total_bad;$('delta').innerHTML='<span class="ok">👍 '+sign(latest.delta_good)+'</span> / <span class="bad">👎 '+sign(latest.delta_bad)+'</span>';$('mood').innerHTML=Number(latest.delta_bad)>0?'<span class="bad">注意</span>':'<span class="ok">OK</span>';showAlert(latest)}else{$('sat').textContent='--%';$('good').textContent='--';$('bad').textContent='--';$('delta').textContent='--';$('mood').textContent='--';showAlert(null)}const s=day0();$('todayD').textContent=deliveries.filter(x=>Number(x.completed_at)>=s).length;$('todayR').textContent=ratings.filter(x=>Number(x.recorded_at)>=s).length;const cand=deliveries.slice(0,3);$('candidate').innerHTML=cand.length?cand.map(x=>'・'+esc(x.store_name||'店舗未入力')+' / '+fmt(x.completed_at)).join('<br>'):'配達完了を押すと候補が出ます。';$('ratingList').innerHTML=ratings.map(x=>'<div class="row"><div class="rowTop"><div><b>👍 '+x.total_good+' / 👎 '+x.total_bad+'</b><div class="hint">'+fmt(x.recorded_at)+'　差分 '+sign(x.delta_good)+' / '+sign(x.delta_bad)+'</div></div><button class="smallDel" onclick="deleteRating(\''+x.id+'\')">削除</button></div></div>').join('')||'<div class="empty">まだありません</div>';$('deliveryList').innerHTML=deliveries.map(x=>'<div class="row"><div class="rowTop"><div><b>'+esc(x.store_name||'店舗未入力')+'</b><div class="hint">'+fmt(x.completed_at)+' '+esc(x.memo||'')+'<br>'+esc(x.area||'')+'</div></div><button class="smallDel" onclick="deleteDelivery(\''+x.id+'\')">削除</button></div></div>').join('')||'<div class="empty">まだありません</div>'}
async function getGpsCandidate(){return new Promise(resolve=>{if(!navigator.geolocation)return resolve({});navigator.geolocation.getCurrentPosition(async pos=>{const lat=pos.coords.latitude,lng=pos.coords.longitude;let place=null;try{const r=await api('/api/reverse-geocode?lat='+encodeURIComponent(lat)+'&lng='+encodeURIComponent(lng));place=r.result||null}catch(e){}resolve({lat,lng,store_name:place?.name||'現在地記録',area:place?.area||'',place})},()=>resolve({}),{enableHighAccuracy:false,timeout:3500})})}
function setSaving(on){['quickSave','quickSave2'].forEach(id=>$(id)?.classList.toggle('saving',on))}
async function saveDelivery(detail=false){if(deliveryLock)return;deliveryLock=true;setSaving(true);buzz([25,35,25]);let gps={};try{if(!detail){toast('GPS取得中...');gps=await getGpsCandidate()}const item={id:crypto.randomUUID(),completed_at:Date.now(),store_name:detail?$('store').value:(gps.store_name||''),area:detail?$('area').value:(gps.area||''),memo:detail?$('memo').value:(gps.place?.display_name?'GPS候補: '+gps.place.display_name:'ワンタップ記録'),lat:gps.lat??null,lng:gps.lng??null};try{await api('/api/deliveries',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(item)});toast('配達を記録しました')}catch(e){deliveries.unshift(item);localSave();toast('端末に保存しました')}await load()}finally{setSaving(false);setTimeout(()=>{deliveryLock=false},900)}}async function fillGps(){toast('GPS取得中...');const gps=await getGpsCandidate();if(gps.store_name)$('store').value=gps.store_name;if(gps.area)$('area').value=gps.area;if(gps.place?.display_name)$('memo').value='GPS候補: '+gps.place.display_name;toast(gps.store_name?'候補を入力しました':'GPS候補を取得できませんでした')}
async function saveRating(g,b,auto=false){g=Number(g||0);b=Number(b||0);if(g+b===0)return toast('数字を確認してください');const prev=ratings[0];const item={id:crypto.randomUUID(),recorded_at:Date.now(),total_good:g,total_bad:b,satisfaction:Math.round(g/(g+b)*100),delta_good:prev?g-Number(prev.total_good||0):0,delta_bad:prev?b-Number(prev.total_bad||0):0};try{await api('/api/ratings',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(item)});toast(auto?'OCRで自動保存しました':'評価を保存しました')}catch(e){ratings.unshift(item);localSave();toast(auto?'OCRで端末保存しました':'端末に保存しました')}await load();if(auto){document.querySelectorAll('.screen').forEach(s=>s.classList.remove('active'));$('home').classList.add('active')}}async function parse(file){$('ocrBox').innerHTML='<div class="hint">OCR解析中...</div>';try{const r=await Tesseract.recognize(file,'eng');const nums=(r.data.text.match(/\d+/g)||[]).map(n=>parseInt(n,10)).filter(Number.isFinite);let g=0,b=0;for(let i=0;i<nums.length-1;i++){if(nums[i]+nums[i+1]===100){g=nums[i];b=nums[i+1];break}}if(!g&&!b&&nums.length>=2){g=nums[nums.length-2];b=nums[nums.length-1]}$('ocrBox').innerHTML='<div class="hint">読み取り：GOOD '+g+' / BAD '+b+'</div><div class="two"><input id="ocrGood" type="number" value="'+g+'"><input id="ocrBad" type="number" value="'+b+'"></div><button id="ocrSave">修正して保存</button>'; $('ocrSave').onclick=()=>saveRating($('ocrGood').value,$('ocrBad').value,false); if(g+b>0){await saveRating(g,b,true)}}catch(e){$('ocrBox').innerHTML='<div class="bad">OCRエラー</div>'}}
async function deleteRating(id){if(!confirm('この評価履歴を削除しますか？'))return;if(dbMode){try{await api('/api/ratings/'+encodeURIComponent(id),{method:'DELETE'})}catch(e){}}ratings=ratings.filter(x=>x.id!==id);localSave();toast('削除しました');await load()}async function deleteDelivery(id){if(!confirm('この配達履歴を削除しますか？'))return;if(dbMode){try{await api('/api/deliveries/'+encodeURIComponent(id),{method:'DELETE'})}catch(e){}}deliveries=deliveries.filter(x=>x.id!==id);localSave();toast('削除しました');await load()}window.deleteRating=deleteRating;window.deleteDelivery=deleteDelivery;
document.querySelectorAll('.tab').forEach(b=>b.onclick=()=>{document.querySelectorAll('.screen').forEach(s=>s.classList.remove('active'));$(b.dataset.tab).classList.add('active')});$('quickSave').onclick=()=>saveDelivery(false);$('quickSave2').onclick=()=>saveDelivery(false);['quickSave','quickSave2'].forEach(id=>{const el=$(id);el.addEventListener('contextmenu',e=>e.preventDefault());el.addEventListener('touchstart',()=>buzz([12]),{passive:true})});$('detailSave').onclick=()=>saveDelivery(true);$('toggleDetail').onclick=()=>$('detail').classList.toggle('open');$('gpsFill').onclick=fillGps;$('pickFromHome').onclick=()=>$('homeFile').click();$('pickImage').onclick=()=>$('imageFile').click();$('homeFile').onchange=e=>{const f=e.target.files[0];if(f){document.querySelectorAll('.screen').forEach(s=>s.classList.remove('active'));$('ocr').classList.add('active');parse(f)}};$('imageFile').onchange=e=>{const f=e.target.files[0];if(f)parse(f)};$('manualSave').onclick=()=>saveRating($('manualGood').value,$('manualBad').value,false);$('clearRatings').onclick=async()=>{if(!confirm('評価履歴をすべて削除しますか？'))return;if(dbMode){try{await api('/api/ratings',{method:'DELETE'})}catch(e){}}ratings=[];localSave();toast('評価履歴を削除しました');await load()};$('clearDeliveries').onclick=async()=>{if(!confirm('配達履歴をすべて削除しますか？'))return;if(dbMode){try{await api('/api/deliveries',{method:'DELETE'})}catch(e){}}deliveries=[];localSave();toast('配達履歴を削除しました');await load()};$('clearAll').onclick=async()=>{if(!confirm('全履歴を削除しますか？'))return;ratings=[];deliveries=[];localSave();toast('全履歴を削除しました');await load()};load();
</script></body></html>`;
}
